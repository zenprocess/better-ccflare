name: Issue Triage Agent

on:
  issues:
    types: [opened]

permissions:
  issues: write
  contents: read

jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Triage issue
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          LLM_URL: ${{ secrets.LLM_URL }}
          # Comma-separated list of models to try in order (fallback support)
          AI_MODELS: "claude-sonnet-4-6,better-ccflare-github-triage,glm-4.7,qwen3-coder-plus,glm-4.5-air"
          AI_TEMPERATURE: "0.3"
          AI_MAX_TOKENS: "5000"
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          # Use toJSON to properly escape all user-provided content
          ISSUE_TITLE: ${{ toJSON(github.event.issue.title) }}
          ISSUE_BODY: ${{ toJSON(github.event.issue.body) }}
          ISSUE_AUTHOR: ${{ toJSON(github.event.issue.user.login) }}
          REPO_NAME: ${{ github.repository }}
        run: |
          bash .github/scripts/issue-triage.sh
